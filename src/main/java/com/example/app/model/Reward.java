package com.example.app.model;

public class Reward {
    public int userId;
    public int points;
    public int level;
    public Reward(int userId, int points, int level) { this.userId = userId; this.points = points; this.level = level; }
}
